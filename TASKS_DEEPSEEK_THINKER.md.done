# Tasks — DeepSeek Thinker (Analysis & Debugging)

## Current State
GPU forward works at 9ms, token 99 produced. NaN/corruption resolved. Focus: output quality analysis and performance optimization.

## Task 1: Analyze output token quality
Token 99 was produced. Determine:
- What prompt was used?
- What does token 99 decode to? (check tokenizer)
- Is this the expected output for the given prompt?
- Compare against reference model output for same prompt

## Task 2: Profile GPU utilization during forward
- What's the actual GPU compute utilization during forwardPartial()?
- Is syncAllThrottled() causing excessive idle time?
- What's the VRAM bandwidth utilization?
- Are all 4 ACE queues being utilized?

## Task 3: Analyze forward pass bottlenecks
9ms per token for 36-layer model. Break down:
- How much time is spent in dequant vs compute vs sync?
- Is the throttling (80% cap) excessive?
- What's the theoretical minimum based on FLOPs?
- Can we overlap dequant with compute?

## Task 4: Verify dequanted weight correctness
Compare GPU dequanted Q values against Python reference:
```
cd C:\Users\rr\Desktop\Notllama-loc
python -c "
from tools.gguf_loader import GGUFLoader
l = GGUFLoader('model/VibeThinker-3B.Q6_K.gguf')
v, s = l.dequantize_q6_k('blk.0.attn_q.weight')
print('First 8:', v[:8])
"
```
If mismatched — investigate dequantize.comp Q6_K path.
