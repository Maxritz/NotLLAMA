--- src/host/web_server.cpp
+++ src/host/web_server.cpp
@@ -117,11 +117,11 @@ static std::string make_sse_chunk(const std::string& data) {
 
 WebServer::WebServer(MultiModelManager* model_mgr,
                      ModelRouter* router,
-                     MTPEngine* mtp,
+                     InferenceRunner* inference,
                      const std::string& host,
                      int32_t port,
                      int32_t timeout_seconds)
-    : model_mgr_(model_mgr), router_(router), mtp_(mtp),
+    : model_mgr_(model_mgr), router_(router), inference_(inference),
       host_(host), port_(port), timeout_seconds_(timeout_seconds) {}
 
 WebServer::~WebServer() { Stop(); }
@@ -735,16 +735,9 @@ CompletionResponse WebServer::DoCompletion(const CompletionRequest& req) {
         prompt_tokens.push_back(static_cast<uint8_t>(c));
     }
 
-    // Set MTP config
-    MTPConfig mtp_cfg;
-    mtp_cfg.n_predict = req.max_tokens > 0 ? req.max_tokens : 128;
-    mtp_cfg.temperature = req.temperature;
-    mtp_->SetConfig(mtp_cfg);
-    if (req.seed >= 0) mtp_->SetSeed(req.seed);
-
-    // Generate
+    // Generate using the real Vulkan inference engine
     uint32_t max_tokens = req.max_tokens > 0 ? static_cast<uint32_t>(req.max_tokens) : 128;
-    auto generated = mtp_->Generate(model_id, prompt_tokens, max_tokens);
+    auto generated = inference_->Generate(model_id, model_mgr_, prompt_tokens, max_tokens);
 
     // Convert tokens to text
     std::string text;
