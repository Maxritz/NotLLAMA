--- a/main.cpp
+++ b/main.cpp
@@ -180,6 +180,11 @@
     if (!engine.initDequantBuffer()) {
         log("dequant buffer init failed");
         std::cerr << "Failed to initialize dequantization buffer\n";
+    }
+    if (!engine.initWeightBuffer()) {
+        log("weight buffer init failed");
+        std::cerr << "Failed to initialize weight buffer (will dequantize per-token)\n";
+    } else {
+        std::cout << "Weight pre-dequantization complete\n";
     }
     if (!engine.initEmbedCache()) {
         log("embed cache init failed (will dequantize per-token)");
