--- a/src/kernels/dequantize.comp
+++ b/src/kernels/dequantize.comp
@@ -55,7 +55,8 @@
 uint readByte(uint addr) {
     uint wordIdx = addr / 4;
     uint byteInWord = addr % 4;
-    return (ByteRef(pc.addrQuant).data[wordIdx] >> (byteInWord * 8)) & 0xFF;
+    uint word = ByteRef(pc.addrQuant).data[wordIdx];
+    return (word >> (byteInWord * 8)) & 0xFF;
 }

 uint16_t readU16(uint addr) {
