--- a/src/host/inference_engine.cpp
+++ b/src/host/inference_engine.cpp
@@ -42,7 +42,9 @@
 InferenceEngine::InferenceEngine(VulkanContext* c, ModelDesc* m, KVCacheManager* k,
     PipelineBuilder* p, Tokenizer* t, Scheduler* s, RingAllocator* a)
     : ctx(c), model(m), kvCache(k), pipelines(p), tokenizer(t), scheduler(s), allocator(a),
-      kernelEntryReady(false) {}
+      kernelEntryReady(false), dequantBuffer(VK_NULL_HANDLE), dequantMemory(VK_NULL_HANDLE),
+      dequantAddr(0), dequantCapacity(0), embedCacheBuffer(VK_NULL_HANDLE),
+      embedCacheMemory(VK_NULL_HANDLE), embedCacheAddr(0), embedCacheSize(0) {}

 bool InferenceEngine::initDequantBuffer() {
     size_t maxSingle = 0;
@@ -187,6 +189,14 @@
     return forwardPartial(tokenId, seqPos, model->blockCount);
 }

+// ============================================================================
+// SINGLE COMMAND BUFFER PER TOKEN
+// All dispatches (dequant + compute) recorded into ONE VkCommandBuffer.
+// vkCmdPipelineBarrier between dependent phases. ONE vkQueueSubmit. ONE sync.
+// Per-layer dequant is preserved (no pre-dequantization — AGENTS.md compliant).
+// Dequant buffer is safely reused across layers via GPU-side barriers.
+// ============================================================================
 uint32_t InferenceEngine::forwardPartial(uint32_t tokenId, uint32_t seqPos, uint32_t maxLayers) {
     uint32_t dim = model->embeddingLength;
     uint32_t hiddenDim = model->feedForwardLength;
@@ -213,20 +223,26 @@
     uint64_t hiddenAddr = hiddenStateAddr + (uint64_t)seqPos * dim * sizeof(float);
     uint64_t lastLogitsAddr = logitsAddr + (uint64_t)lastLogitsOffset * model->vocabSize * sizeof(float);

+    // === START ONE COMMAND BUFFER FOR ENTIRE FORWARD PASS ===
+    scheduler->beginBatch(0);
+
     // === Embedding ===
     uint64_t embedAddr = findTensorAddr(*model, "token_embd.weight");
     if (embedAddr) {
         uint64_t embedAddr_dq = 0;
         if (embedCacheReady) {
             embedAddr_dq = embedCacheAddr;
         } else {
-            embedAddr_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, "token_embd.weight", 0, false);
+            embedAddr_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, "token_embd.weight", 0, true);
         }
         EmbedPushConstants embedPC = {embedAddr_dq ? embedAddr_dq : embedAddr, hiddenAddr, tokenId, seqPos, dim};
-        scheduler->dispatch(pipelines->getPipeline("embed"), pipelines->getLayout("embed"),
+        scheduler->dispatchInBatch(pipelines->getPipeline("embed"), pipelines->getLayout("embed"),
             &embedPC, sizeof(embedPC), (dim + 31) / 32, 1, 1);
-        scheduler->syncAllThrottled(0.8);
+        scheduler->barrierBetweenGroups(
+            VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
+            VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);
     }

     uint32_t layersToRun = std::min(maxLayers, model->blockCount);
@@ -234,13 +250,20 @@
     for (uint32_t layer = 0; layer < layersToRun; ++layer) {
         std::string prefix = "blk." + std::to_string(layer);

-        uint64_t addrAttnNorm_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_norm.weight", 0, false);
-        uint64_t addrQW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_q.weight", 0, false);
-        uint64_t addrKW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_k.weight", qSize, false);
-        uint64_t addrVW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_v.weight", qSize + kSize, false);
-        uint64_t addrOW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_output.weight", qSize + kSize + vSize, false);
-        scheduler->syncAllThrottled(0.8);
+        // === Attention Dequant (recorded into same cmd buffer) ===
+        uint64_t addrAttnNorm_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_norm.weight", 0, true);
+        uint64_t addrQW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_q.weight", 0, true);
+        uint64_t addrKW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_k.weight", qSize, true);
+        uint64_t addrVW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_v.weight", qSize + kSize, true);
+        uint64_t addrOW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".attn_output.weight", qSize + kSize + vSize, true);
+
+        // Barrier: dequant writes must complete before GEMM reads
+        scheduler->barrierBetweenGroups(
+            VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
+            VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);

         uint64_t addrAttnNorm = findTensorAddr(*model, prefix + ".attn_norm.weight");
         uint64_t addrQW = findTensorAddr(*model, prefix + ".attn_q.weight");
@@ -248,8 +271,8 @@
         uint64_t addrVW = findTensorAddr(*model, prefix + ".attn_v.weight");
         uint64_t addrOW = findTensorAddr(*model, prefix + ".attn_output.weight");

-        scheduler->beginBatch(0);
         RmsNormPushConstants normPC = {hiddenAddr, addrAttnNorm_dq ? addrAttnNorm_dq : addrAttnNorm, attnOutAddr, dim, 1, 1e-6f};
-        scheduler->dispatchInBatch(pipelines->getPipeline("rms_norm"), pipelines->getLayout("rms_norm"), &normPC, sizeof(normPC), 1, 1, 1);
+        scheduler->dispatchInBatch(pipelines->getPipeline("rms_norm"), pipelines->getLayout("rms_norm"),
+            &normPC, sizeof(normPC), 1, 1, 1);

         uint64_t normHidden = attnOutAddr;
         GemmPushConstants qPC = {normHidden, addrQW_dq ? addrQW_dq : addrQW, qRowAddr, 1, dim, dim, 1.0f, 0};
@@ -262,8 +285,9 @@
         scheduler->dispatchInBatch(pipelines->getPipeline("gemm"), pipelines->getLayout("gemm"), &vPC, sizeof(vPC), ((uint32_t)(headDim*model->headCountKv)+31)/32, 1, 1);

         scheduler->barrierBetweenGroups(
             VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
             VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);

         uint32_t ropeTotal = model->headCount * headDim;
         RopePushConstants ropePC = {qRowAddr, kRowAddr, seqLen, headDim, model->headCount, model->headCountKv, 10000.0f, 1.0f};
-        scheduler->dispatchInBatch(pipelines->getPipeline("rope"), pipelines->getLayout("rope"), &ropePC, sizeof(ropePC), (ropeTotal + 31) / 32, 1, 1);
+        scheduler->dispatchInBatch(pipelines->getPipeline("rope"), pipelines->getLayout("rope"),
+            &ropePC, sizeof(ropePC), (ropeTotal + 31) / 32, 1, 1);

         uint64_t kCacheAddr = kvCache->getKBufferAddress(layer);
         uint64_t vCacheAddr = kvCache->getVBufferAddress(layer);
@@ -276,8 +300,9 @@
         for (uint32_t h = 0; h < model->headCount; ++h) {
             AttentionPushConstants attnPC = {qAddr, kCacheAddr, vCacheAddr, attnOutAddr, seqLen, headDim, model->headCount, model->headCountKv, h, 1.0f / std::sqrt(static_cast<float>(headDim))};
-            scheduler->dispatchInBatch(pipelines->getPipeline("attention"), pipelines->getLayout("attention"), &attnPC, sizeof(attnPC), 1, 1, 1);
+            scheduler->dispatchInBatch(pipelines->getPipeline("attention"), pipelines->getLayout("attention"),
+                &attnPC, sizeof(attnPC), 1, 1, 1);
         }

         scheduler->barrierBetweenGroups(
             VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
             VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);

         GemmPushConstants outPC = {attnOutAddr, addrOW_dq ? addrOW_dq : addrOW, mlpOutAddr, 1, dim, dim, 1.0f, 0};
-        scheduler->dispatchInBatch(pipelines->getPipeline("gemm"), pipelines->getLayout("gemm"), &outPC, sizeof(outPC), (dim+31)/32, 1, 1);
+        scheduler->dispatchInBatch(pipelines->getPipeline("gemm"), pipelines->getLayout("gemm"),
+            &outPC, sizeof(outPC), (dim+31)/32, 1, 1);

         AddPushConstants addPC1 = {hiddenAddr, mlpOutAddr, hiddenAddr, dim};
         scheduler->dispatchInBatch(pipelines->getPipeline("add"), pipelines->getLayout("add"), &addPC1, sizeof(addPC1), (dim+255)/256, 1, 1);

-        scheduler->endBatch(VK_NULL_HANDLE);
-        scheduler->syncAll();
+        // Barrier: attention done → FFN dequant can overwrite buffer
+        scheduler->barrierBetweenGroups(
+            VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
+            VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_WRITE_BIT);

-        uint64_t addrFfnNorm = findTensorAddr(*model, prefix + ".ffn_norm.weight");
-        uint64_t addrFfnNorm_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_norm.weight", 0, false);
+        // === FFN Dequant (recorded into same cmd buffer, overwrites attention weights) ===
+        uint64_t addrFfnNorm_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_norm.weight", 0, true);

         size_t upSize = getF16OutputSize(*model, prefix + ".ffn_up.weight");
         size_t gateSize = getF16OutputSize(*model, prefix + ".ffn_gate.weight");

-        uint64_t addrUpW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_up.weight", 0, false);
-        uint64_t addrGateW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_gate.weight", upSize, false);
-        uint64_t addrDownW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_down.weight", upSize + gateSize, false);
-        scheduler->syncAllThrottled(0.8);
+        uint64_t addrUpW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_up.weight", 0, true);
+        uint64_t addrGateW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_gate.weight", upSize, true);
+        uint64_t addrDownW_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, prefix + ".ffn_down.weight", upSize + gateSize, true);
+
+        // Barrier: FFN dequant writes must complete before GEMM reads
+        scheduler->barrierBetweenGroups(
+            VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
+            VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);

-        scheduler->beginBatch(0);
+        uint64_t addrFfnNorm = findTensorAddr(*model, prefix + ".ffn_norm.weight");
         RmsNormPushConstants ffnNormPC = {hiddenAddr, addrFfnNorm_dq ? addrFfnNorm_dq : addrFfnNorm, attnOutAddr, dim, 1, 1e-6f};
-        scheduler->dispatchInBatch(pipelines->getPipeline("rms_norm"), pipelines->getLayout("rms_norm"), &ffnNormPC, sizeof(ffnNormPC), 1, 1, 1);
+        scheduler->dispatchInBatch(pipelines->getPipeline("rms_norm"), pipelines->getLayout("rms_norm"),
+            &ffnNormPC, sizeof(ffnNormPC), 1, 1, 1);

         scheduler->barrierBetweenGroups(
             VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
             VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);

         uint64_t addrUpW = findTensorAddr(*model, prefix + ".ffn_up.weight");
         uint64_t addrGateW = findTensorAddr(*model, prefix + ".ffn_gate.weight");
         uint64_t addrDownW = findTensorAddr(*model, prefix + ".ffn_down.weight");

         MlpFusedGateUpPushConstants fusedPC = {attnOutAddr, addrGateW_dq ? addrGateW_dq : addrGateW, addrUpW_dq ? addrUpW_dq : addrUpW, addrDownW_dq ? addrDownW_dq : addrDownW, mlpOutAddr, dim, hiddenDim};
-        scheduler->dispatchInBatch(pipelines->getPipeline("mlp_fused_gateup"), pipelines->getLayout("mlp_fused_gateup"), &fusedPC, sizeof(fusedPC), (dim+31)/32, 1, 1);
+        scheduler->dispatchInBatch(pipelines->getPipeline("mlp_fused_gateup"), pipelines->getLayout("mlp_fused_gateup"),
+            &fusedPC, sizeof(fusedPC), (dim+31)/32, 1, 1);

         AddPushConstants addPC2 = {hiddenAddr, mlpOutAddr, hiddenAddr, dim};
         scheduler->dispatchInBatch(pipelines->getPipeline("add"), pipelines->getLayout("add"), &addPC2, sizeof(addPC2), (dim+255)/256, 1, 1);

-        scheduler->endBatch(VK_NULL_HANDLE);
-        scheduler->syncAll();
+        // Barrier: FFN done → next layer can overwrite dequant buffer
+        scheduler->barrierBetweenGroups(
+            VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
+            VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_WRITE_BIT);

-        kvCache->incrementSeqLen(layer);
+        // NO CPU SYNC HERE — next layer continues in same cmd buffer
     }

+    // === Final RMS Norm ===
     uint64_t addrOutNorm = findTensorAddr(*model, "output_norm.weight");
-    uint64_t addrOutNorm_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, "output_norm.weight", 0, false);
+    uint64_t addrOutNorm_dq = dequantWeight(scheduler, pipelines, dequantAddr, dequantCapacity, *model, "output_norm.weight", 0, true);

-    scheduler->beginBatch(0);
     RmsNormPushConstants outNormPC = {hiddenAddr, addrOutNorm_dq ? addrOutNorm_dq : addrOutNorm, attnOutAddr, dim, 1, 1e-6f};
-    scheduler->dispatchInBatch(pipelines->getPipeline("rms_norm"), pipelines->getLayout("rms_norm"), &outNormPC, sizeof(outNormPC), 1, 1, 1);
+    scheduler->dispatchInBatch(pipelines->getPipeline("rms_norm"), pipelines->getLayout("rms_norm"),
+        &outNormPC, sizeof(outNormPC), 1, 1, 1);

     scheduler->barrierBetweenGroups(
         VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
         VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);

     uint64_t addrLMHead = findTensorAddr(*model, "output.weight");
     GemmPushConstants lmPC = {attnOutAddr, addrLMHead, lastLogitsAddr, 1, model->vocabSize, dim, 1.0f, 0};
-    scheduler->dispatchInBatch(pipelines->getPipeline("gemm"), pipelines->getLayout("gemm"), &lmPC, sizeof(lmPC), (model->vocabSize + 31) / 32, 1, 1);
+    scheduler->dispatchInBatch(pipelines->getPipeline("gemm"), pipelines->getLayout("gemm"),
+        &lmPC, sizeof(lmPC), (model->vocabSize + 31) / 32, 1, 1);

     scheduler->barrierBetweenGroups(
         VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT, VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT,
         VK_ACCESS_SHADER_WRITE_BIT, VK_ACCESS_SHADER_READ_BIT);

     uint32_t seed = static_cast<uint32_t>(std::chrono::steady_clock::now().time_since_epoch().count());
     TopKPushConstants topkPC = {lastLogitsAddr, outputTokenAddr, scratchAddr, model->vocabSize, 1.0f, 40, 0.9f, seed};
-    scheduler->dispatchInBatch(pipelines->getPipeline("topk"), pipelines->getLayout("topk"), &topkPC, sizeof(topkPC), 1, 1, 1);
+    scheduler->dispatchInBatch(pipelines->getPipeline("topk"), pipelines->getLayout("topk"),
+        &topkPC, sizeof(topkPC), 1, 1, 1);

+    // === ONE SUBMIT + ONE SYNC FOR ENTIRE FORWARD PASS ===
     scheduler->endBatch(VK_NULL_HANDLE);
     scheduler->syncAll();

+    // Increment KV cache seq lens AFTER GPU is done (for next token)
+    for (uint32_t layer = 0; layer < layersToRun; ++layer) {
+        kvCache->incrementSeqLen(layer);
+    }
+
     // Read back sampled token
     uint32_t sampledToken = 0;
     if (logitsMapped) {
