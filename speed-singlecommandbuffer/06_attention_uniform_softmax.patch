--- a/src/kernels/attention.comp
+++ b/src/kernels/attention.comp
@@ -95,6 +95,9 @@
         }
     }

+    m = subgroupBroadcastFirst(m);
+    l = subgroupBroadcastFirst(l);
+
     float invL = 1.0 / l;

     for (uint d = tid; d < headDim; d += 32) {
